# config/nav2

Navigation2 실행에 필요한 파라미터 파일을 둔다.

예정 파일:

- `nav2_params.yaml`: planner, controller, costmap, behavior server 설정
- `amcl_params.yaml`: 저장된 map 기반 localization 설정
- `slamnav_params.yaml`: Go2 SLAM/Nav 통합 실행용 설정

